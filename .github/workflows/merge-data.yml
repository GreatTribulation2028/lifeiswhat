# DISABLED — this workflow previously auto-merged data files.
# It has been disabled because data is now manually managed via data_0000.js.
# The workflow was overwriting data_merged.js with empty content.
# To re-enable, restore the original triggers below.

name: Merge Data Files (DISABLED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type CONFIRM to run'
        required: true

jobs:
  merge:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled"